import Image from 'next/image'
function BoardImage({src, alt, width, height, className, text, textClassname}) {

    return (
        <div className="relative">
            hello
        </div>
    );
}

export default BoardImage;